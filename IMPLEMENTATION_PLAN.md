# 📋 План реализации новых требований заказчика

## 🎯 Обзор задач

1. **Добавление казахского языка** - поддержка двуязычности для учеников
2. **Случайный тест из 30 вопросов** - новая кнопка в главном меню
3. **Удаление индикаторов источника вопросов** - убрать упоминания ИИ/БД
4. **Исправление ИИ генерации объяснений** - проверить и починить

---

## 🚀 ЗАДАЧА 1: Добавление казахского языка

### 1.1 Обновление структуры базы данных

**Файлы для изменения:**
- `src/services/database.py`

**Действия:**
1. ~~Добавить поле `language` в таблицу `questions`~~ **ОТМЕНЕНО** - язык определяется через тему
2. Добавить поле `language` в таблицу `subtopics` для привязки тем к языку
3. Создать миграцию для существующих данных (установить 'ru' по умолчанию)
4. **НОВОЕ**: Добавить методы очистки `user_errors` и `test_results` при смене языка пользователя

```sql
-- Новые поля в таблицах:
ALTER TABLE subtopics ADD COLUMN language TEXT DEFAULT 'ru';

-- Обновление существующих записей
UPDATE subtopics SET language = 'ru' WHERE language IS NULL;
```

### 1.2 Обновление PDF процессора

**Файлы для изменения:**
- `src/services/pdf_processor.py`

**Действия:**
1. ~~Улучшить функцию `detect_language()` для более точного определения казахского языка~~ **ИЗМЕНЕНО**
2. **НОВОЕ**: Добавить диалог выбора языка при загрузке PDF в админ-панели
3. При добавлении вопросов через админ-панель сохранять выбранный язык в темы
4. Убрать автоматическое определение языка - использовать выбор админа

### 1.3 Обновление системы тем

**Файлы для изменения:**
- `src/config/constants.py`
- `src/services/database.py`
- `src/services/topic_manager.py`

**Действия:**
1. Создать `TOPIC_HIERARCHY_KK` с казахскими названиями тем
2. Обновить методы получения тем с фильтрацией по языку пользователя
3. Добавить методы для работы с темами по языкам
4. **НОВОЕ**: При добавлении темы через админ-панель добавить выбор языка
5. **НОВОЕ**: В списке тем для админов показывать язык в скобках (ru/kz)

### 1.4 Обновление пользовательского интерфейса

**Файлы для изменения:**
- `src/utils/keyboards.py`
- `src/handlers/callback_handlers.py`
- `src/handlers/command_handlers.py`
- `src/handlers/admin_handlers.py`

**Действия:**
1. Фильтровать темы по языку пользователя при построении клавиатур
2. Показывать только темы на языке пользователя (для учеников)
3. **НОВОЕ**: Для админов показывать все темы с индикатором языка (ru/kz)
4. **НОВОЕ**: Добавить диалог выбора языка при создании темы
5. **НОВОЕ**: При смене языка пользователя очищать его историю ошибок и результатов

### 1.5 Обновление системы вопросов

**Файлы для изменения:**
- `src/services/question_service.py`
- `src/services/database.py`

**Действия:**
1. Фильтровать вопросы по языку пользователя через связь с темами
2. Обновить методы получения случайных вопросов с учетом языка
3. Обновить ИИ генерацию для казахского языка
4. **НОВОЕ**: Добавить методы очистки пользовательских данных при смене языка

---

## 🎲 ЗАДАЧА 2: Случайный тест из 30 вопросов

### 2.1 Обновление главного меню

**Файлы для изменения:**
- `src/config/constants.py`

**Действия:**
1. Добавить кнопку "🎯 Начать тест (30 вопросов)" в `MAIN_MENU_KEYBOARD`

```python
MAIN_MENU_KEYBOARD = [
    ["📚 Выбрать тему и начать"],
    ["🎯 Начать тест (30 вопросов)"],  # НОВАЯ КНОПКА
    ["📊 Мой прогресс"],
    ["❓ Помощь"]
]
```

### 2.2 Создание сервиса случайных тестов

**Новый файл:**
- `src/services/random_test_service.py`

**Функционал:**
1. `generate_random_test(user_id, question_count=30)` - генерация случайного теста с учетом языка пользователя
2. `generate_retry_test(user_id, failed_topics)` - тест на основе ошибок
3. `get_balanced_questions_from_topics(topics, count)` - равномерное распределение вопросов
4. **НОВОЕ**: Фильтрация вопросов по языку пользователя через темы

### 2.3 Обновление обработчиков

**Файлы для изменения:**
- `src/handlers/command_handlers.py`
- `src/handlers/callback_handlers.py`

**Действия:**
1. Добавить обработчик кнопки "🎯 Начать тест (30 вопросов)"
2. Создать логику случайного теста с учетом языка пользователя
3. Добавить кнопку "🔄 Пройти еще раз" с логикой повторения ошибок

### 2.4 Обновление системы результатов

**Файлы для изменения:**
- `src/services/database.py`
- `src/utils/keyboards.py`

**Действия:**
1. Сохранять результаты случайных тестов отдельно
2. Отслеживать темы с ошибками для повторного прохождения
3. Создать специальную клавиатуру для результатов случайного теста

---

## 🎨 ЗАДАЧА 3: Удаление индикаторов источника вопросов

### 3.1 Обновление клавиатур

**Файлы для изменения:**
- `src/utils/keyboards.py`

**Действия:**
1. Убрать текст "🟢" и "🟡" из кнопок тем
2. Убрать упоминания "(ИИ)" и индикаторы источника
3. **ИЗМЕНЕНО**: Убрать количество вопросов из интерфейса ученика
4. **НОВОЕ**: Для админов оставить количество вопросов и добавить индикатор языка

**Было:**
```python
button_text = f"🟢 {subtopic_name} ({question_count})"
button_text = f"🟡 {subtopic_name} (ИИ)"
```

**Станет для учеников:**
```python
button_text = f"{subtopic_name}"  # Только название темы
```

**Станет для админов:**
```python
button_text = f"{subtopic_name} ({question_count}) [{language}]"  # С количеством и языком
```

### 3.2 Обновление объяснений и текстов

**Файлы для изменения:**
- `src/handlers/callback_handlers.py`
- `src/handlers/admin_handlers.py`
- `src/config/constants.py`

**Действия:**
1. Убрать все упоминания "из базы данных" или "сгенерировано ИИ"
2. Убрать объяснения про желтые и зеленые кружочки
3. Оставить только цветные индикаторы без объяснений

---

## 🤖 ЗАДАЧА 4: Исправление ИИ генерации объяснений

### 4.1 Диагностика проблемы

**Файлы для проверки:**
- `src/services/ai_service.py`
- `src/services/pdf_processor.py`

**Действия:**
1. Проверить работу метода `generate_explanation()`
2. Проверить интеграцию с Gemini API
3. Проверить сохранение объяснений в БД при обработке PDF

### 4.2 Исправление ИИ сервиса

**Файлы для изменения:**
- `src/services/ai_service.py`

**Возможные проблемы и решения:**
1. Проблемы с API ключом - проверить переменные окружения
2. Проблемы с промптом - обновить промпт для объяснений
3. Проблемы с обработкой ответа - улучшить парсинг ответа ИИ

### 4.3 Обновление PDF процессора

**Файлы для изменения:**
- `src/services/pdf_processor.py`

**Действия:**
1. Убедиться что объяснения генерируются для всех вопросов из PDF
2. Добавить логирование процесса генерации объяснений
3. Добавить обработку ошибок при генерации объяснений

---

## 📝 ПЛАН ВЫПОЛНЕНИЯ ПО ЭТАПАМ

### Этап 1: Подготовка (1 сессия)
1. Создать резервную копию базы данных
2. Обновить структуру БД для поддержки языков в темах
3. Создать константы для казахского языка

### Этап 2: Казахский язык (2-3 сессии)
1. **НОВОЕ**: Добавить диалог выбора языка при загрузке PDF
2. **НОВОЕ**: Добавить диалог выбора языка при создании тем
3. Создать казахские переводы тем
4. Обновить фильтрацию по языкам через темы
5. **НОВОЕ**: Реализовать очистку данных при смене языка пользователя
6. Тестировать двуязычность

### Этап 3: Случайный тест (2 сессии)
1. Создать сервис случайных тестов с поддержкой языков
2. Добавить кнопку в главное меню
3. Реализовать логику повторения ошибок
4. Тестировать функционал

### Этап 4: Очистка интерфейса (1 сессия)
1. Убрать индикаторы источника вопросов
2. **НОВОЕ**: Убрать количество вопросов из интерфейса ученика
3. **НОВОЕ**: Добавить индикаторы языка для админов
4. Очистить тексты от упоминаний ИИ/БД

### Этап 5: Исправление ИИ (1 сессия)
1. Диагностировать проблему с ИИ
2. Исправить генерацию объяснений
3. Протестировать на PDF файлах

### Этап 6: Тестирование и финализация (1 сессия)
1. Полное тестирование всех функций
2. Обновление документации
3. Коммит изменений

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Новые файлы для создания:
1. `src/services/random_test_service.py` - сервис случайных тестов
2. `src/config/constants_kk.py` - константы для казахского языка
3. `src/utils/language_utils.py` - утилиты для работы с языками

### Основные изменения в существующих файлах:
1. `src/services/database.py` - добавление поля языка в темы, методы очистки данных
2. `src/config/constants.py` - новая кнопка меню, казахские темы
3. `src/utils/keyboards.py` - фильтрация по языку, разные интерфейсы для учеников/админов
4. `src/handlers/command_handlers.py` - обработчик случайного теста, очистка при смене языка
5. `src/handlers/callback_handlers.py` - логика случайного теста
6. `src/handlers/admin_handlers.py` - диалоги выбора языка для PDF и тем
7. `src/services/ai_service.py` - исправление генерации объяснений
8. `src/services/pdf_processor.py` - диалог выбора языка вместо автоопределения

### Миграции БД:
```sql
-- Добавление языка к подтемам  
ALTER TABLE subtopics ADD COLUMN language TEXT DEFAULT 'ru';

-- Обновление существующих записей
UPDATE subtopics SET language = 'ru' WHERE language IS NULL;
```

### Новые методы БД:
```python
# Очистка данных пользователя при смене языка
def clear_user_data_on_language_change(self, user_id: int) -> None:
    """Очищает user_errors и test_results при смене языка пользователя."""
    
# Получение тем с фильтрацией по языку
def get_topics_by_language(self, language: str, active_only: bool = True) -> List[Dict]:
    """Получает темы для конкретного языка."""
    
# Получение вопросов по языку через темы
def get_questions_by_user_language(self, user_id: int, topic: str = None) -> List[Dict]:
    """Получает вопросы на языке пользователя через связь с темами."""
```

---

## ✅ КРИТЕРИИ ГОТОВНОСТИ

### Задача 1 - Казахский язык:
- [ ] Пользователи видят только темы на своем языке
- [ ] Вопросы показываются на языке пользователя через темы
- [ ] **НОВОЕ**: При загрузке PDF админ выбирает язык
- [ ] **НОВОЕ**: При создании темы админ выбирает язык
- [ ] **НОВОЕ**: Админы видят язык темы в скобках (ru/kz)
- [ ] **НОВОЕ**: При смене языка пользователя очищаются его ошибки и результаты

### Задача 2 - Случайный тест:
- [ ] Кнопка "Начать тест" в главном меню работает
- [ ] Генерируется 30 случайных вопросов из разных тем на языке пользователя
- [ ] "Пройти еще раз" дает вопросы по ошибкам равномерно
- [ ] Результаты сохраняются корректно

### Задача 3 - Очистка интерфейса:
- [ ] Убраны все упоминания "ИИ" и "БД" 
- [ ] Убраны объяснения про цветные кружочки
- [ ] **НОВОЕ**: Ученики видят только названия тем без количества вопросов
- [ ] **НОВОЕ**: Админы видят темы с количеством вопросов и языком
- [ ] Интерфейс выглядит чисто и профессионально

### Задача 4 - ИИ объяснения:
- [ ] ИИ генерирует объяснения для вопросов из PDF
- [ ] Объяснения сохраняются в базу данных
- [ ] Нет ошибок при обработке PDF файлов

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Скопируйте этот план** в следующий чат
2. **Укажите номер задачи** (1, 2, 3 или 4) которую хотите реализовать первой
3. **Я выполню задачу** согласно плану с детальными комментариями
4. **Повторите** для остальных задач

**Рекомендуемый порядок выполнения:** 4 → 3 → 1 → 2
(сначала простые, потом сложные)

---

## 🔄 КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ В ПЛАНЕ

### ✅ Исправлено согласно требованиям:
1. **Убрано поле language из questions** - язык определяется через связь с темами
2. **Добавлен выбор языка при загрузке PDF** - админ выбирает язык вместо автоопределения
3. **Добавлен выбор языка при создании тем** - с отображением языка для админов
4. **Добавлена очистка данных при смене языка** - user_errors и test_results очищаются
5. **Убрано количество вопросов для учеников** - только названия тем
6. **Разные интерфейсы для ролей** - ученики видят простые названия, админы - детальную информацию

---

*План обновлен: Январь 2025*
*Статус: Готов к выполнению с корректировками*