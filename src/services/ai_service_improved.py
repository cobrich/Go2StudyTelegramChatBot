import logging
import google.generativeai as genai
from typing import Optional, Tuple, List
import re
from src.config.constants import GEMINI_API_KEY, GEMINI_MODEL, MAX_OPTION_LENGTH

class ImprovedAIService:
    def __init__(self):
        genai.configure(api_key=GEMINI_API_KEY)
        self.model = genai.GenerativeModel(GEMINI_MODEL)

    def generate_task_v2(self, topic: str, main_topic: str = None, language: str = 'ru') -> Tuple[Optional[str], Optional[str], Optional[List[str]], Optional[str]]:
        """
        Улучшенная генерация задач с семантическим анализом темы.
        Вариант 1: Семантический анализ с примерами
        """
        # Формируем контекст темы
        if main_topic:
            clean_main_topic = main_topic.split(' ', 1)[-1] if ' ' in main_topic else main_topic
            if language == 'kk':
                topic_context = f"'{topic}' тақырыбы '{clean_main_topic}' бөлімінен"
            else:
                topic_context = f"Тема '{topic}' из раздела '{clean_main_topic}'"
        else:
            if language == 'kk':
                topic_context = f"'{topic}' тақырыбы"
            else:
                topic_context = f"Тема '{topic}'"

        if language == 'kk':
            prompt_template = self._get_kazakh_semantic_prompt(topic, topic_context)
        else:
            prompt_template = self._get_russian_semantic_prompt(topic, topic_context)

        try:
            response_text = self.model.generate_content(prompt_template).text
            return self._parse_response(response_text, language)
        except Exception as err:
            logging.error(f"Error generating AI task: {err}")
            return None, None, None, None

    def _get_russian_semantic_prompt(self, topic: str, topic_context: str) -> str:
        """Создает семантический промпт для русского языка"""
        return f"""Ты - эксперт по математике для 5-6 классов. Твоя задача - создать ОДИН математический вопрос строго по теме: {topic_context}.

КРИТИЧЕСКИ ВАЖНО - СЕМАНТИЧЕСКИЙ АНАЛИЗ ТЕМЫ:
Проанализируй название темы "{topic}" и определи:
1. КЛЮЧЕВЫЕ МАТЕМАТИЧЕСКИЕ ПОНЯТИЯ в названии темы
2. ТИПЫ ОПЕРАЦИЙ, которые должны быть в вопросе
3. ФОРМАТ ОТВЕТА, который ожидается
4. ОГРАНИЧЕНИЯ на числа и операции

ПРИМЕРЫ СЕМАНТИЧЕСКОГО АНАЛИЗА:
- "Натуральные числа" → используй только 1,2,3,4,5..., ответ - натуральное число
- "Проценты" → обязательно символ %, операции с процентами, ответ содержит %
- "Упрощение выражений" → алгебраические выражения с переменными, ответ - упрощенное выражение
- "Площадь и периметр" → геометрические фигуры, формулы площади/периметра, ответ с единицами измерения
- "Десятичные дроби" → числа с запятой (0.5, 1.25), ответ - десятичная дробь
- "Целые числа" → включая отрицательные и ноль (...,-2,-1,0,1,2...), ответ - целое число

АЛГОРИТМ СОЗДАНИЯ ВОПРОСА:
1. Извлеки из названия темы "{topic}" главные математические понятия
2. Определи, какие операции и числа ДОЛЖНЫ присутствовать в вопросе
3. Создай вопрос, который НЕВОЗМОЖНО решить без использования этих понятий
4. Убедись, что ответ имеет правильный формат для данной темы
5. Проверь: можно ли отнести этот вопрос к другой теме? Если да - переделай!

ТРЕБОВАНИЯ К ВОПРОСУ:
- Уровень: 5-6 класс
- Решение: 2-3 логических шага максимум
- Контекст: небольшая текстовая задача или практическая ситуация
- Уникальность: избегай шаблонных формулировок

СТРОГИЙ ФОРМАТ ОТВЕТА:
ВОПРОС: [текст вопроса]
ПРАВИЛЬНЫЙ ОТВЕТ: [правильный ответ]
НЕПРАВИЛЬНЫЙ ОТВЕТ 1: [первый неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 2: [второй неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 3: [третий неправильный вариант]
ОБЪЯСНЕНИЕ: [объяснение решения]

САМОПРОВЕРКА ПЕРЕД ОТВЕТОМ:
- Содержит ли вопрос ключевые понятия из темы "{topic}"?
- Невозможно ли решить вопрос без знания этой темы?
- Соответствует ли формат ответа теме?
- Подходит ли вопрос для 5-6 класса?"""

    def _get_kazakh_semantic_prompt(self, topic: str, topic_context: str) -> str:
        """Создает семантический промпт для казахского языка"""
        return f"""Сіз 5-6 сынып оқушыларына арналған математика сарапшысысыз. Сіздің міндетіңіз - {topic_context} тақырыбы бойынша БІР математикалық сұрақ жасау.

ӨТЕ МАҢЫЗДЫ - ТАҚЫРЫПТЫҢ СЕМАНТИКАЛЫҚ ТАЛДАУЫ:
"{topic}" тақырыбының атауын талдап, анықтаңыз:
1. Тақырып атауындағы НЕГІЗГІ МАТЕМАТИКАЛЫҚ ҰҒЫМДАР
2. Сұрақта болуы тиіс АМАЛДАР ТҮРЛЕРІ
3. Күтілетін ЖАУАП ФОРМАТЫ
4. Сандар мен амалдарға ШЕКТЕУЛЕР

СЕМАНТИКАЛЫҚ ТАЛДАУ МЫСАЛДАРЫ:
- "Натурал сандар" → тек 1,2,3,4,5... қолдану, жауап - натурал сан
- "Пайыздар" → міндетті % белгісі, пайыздармен амалдар, жауапта %
- "Өрнектерді жеңілдету" → айнымалылары бар алгебралық өрнектер, жауап - жеңілдетілген өрнек
- "Аудан және периметр" → геометриялық фигуралар, аудан/периметр формулалары, өлшем бірліктері
- "Ондық бөлшектер" → үтірлі сандар (0.5, 1.25), жауап - ондық бөлшек
- "Бүтін сандар" → теріс және нөлді қоса (...,-2,-1,0,1,2...), жауап - бүтін сан

СҰРАҚ ЖАСАУ АЛГОРИТМІ:
1. "{topic}" тақырыбының атауынан негізгі математикалық ұғымдарды бөліп алыңыз
2. Сұрақта қандай амалдар мен сандар БОЛУЫ ТИІС екенін анықтаңыз
3. Осы ұғымдарды қолданбай шешу МҮМКІН ЕМЕС сұрақ жасаңыз
4. Жауаптың осы тақырыпқа дұрыс форматта екенін тексеріңіз
5. Тексеру: бұл сұрақты басқа тақырыпқа жатқызуға бола ма? Егер болса - қайта жасаңыз!

СҰРАҚҚА ҚОЙЫЛАТЫН ТАЛАПТАР:
- Деңгей: 5-6 сынып
- Шешім: максимум 2-3 логикалық қадам
- Контекст: кішкентай мәтінді есеп немесе практикалық жағдай
- Бірегейлік: шаблондық тұжырымдамалардан аулақ болыңыз

ҚАТАҢ ЖАУАП ФОРМАТЫ:
СҰРАҚ: [сұрақ мәтіні]
ДҰРЫС ЖАУАП: [дұрыс жауап]
ҚАТЕ ЖАУАП 1: [бірінші қате нұсқа]
ҚАТЕ ЖАУАП 2: [екінші қате нұсқа]
ҚАТЕ ЖАУАП 3: [үшінші қате нұсқа]
ТҮСІНДІРМЕ: [шешімнің түсіндірмесі]

ЖАУАП АЛДЫНДА ӨЗІН-ӨЗІ ТЕКСЕРУ:
- Сұрақта "{topic}" тақырыбының негізгі ұғымдары бар ма?
- Бұл тақырыпты білмей сұрақты шешу мүмкін емес пе?
- Жауап форматы тақырыпқа сәйкес пе?
- Сұрақ 5-6 сыныпқа жарамды ма?"""

    def generate_task_v3(self, topic: str, main_topic: str = None, language: str = 'ru') -> Tuple[Optional[str], Optional[str], Optional[List[str]], Optional[str]]:
        """
        Вариант 2: Контекстуальный анализ с автоматическим определением требований
        """
        if language == 'kk':
            prompt = self._get_contextual_kazakh_prompt(topic, main_topic)
        else:
            prompt = self._get_contextual_russian_prompt(topic, main_topic)

        try:
            response_text = self.model.generate_content(prompt).text
            return self._parse_response(response_text, language)
        except Exception as err:
            logging.error(f"Error generating AI task: {err}")
            return None, None, None, None

    def _get_contextual_russian_prompt(self, topic: str, main_topic: str) -> str:
        """Контекстуальный промпт с автоматическим определением требований"""
        return f"""Ты - эксперт по созданию математических задач для 5-6 классов.

ЗАДАЧА: Создать вопрос по теме "{topic}" из раздела "{main_topic}".

ЭТАП 1 - АВТОМАТИЧЕСКИЙ АНАЛИЗ ТЕМЫ:
Проанализируй тему "{topic}" и автоматически определи:

А) МАТЕМАТИЧЕСКАЯ ОБЛАСТЬ:
- Если в названии есть слова "числа", "натуральные", "целые" → работа с числами определенного типа
- Если есть "дроби", "десятичные", "обыкновенные" → работа с дробями
- Если есть "проценты" → обязательно использование %
- Если есть "выражения", "упрощение", "скобки" → алгебраические преобразования
- Если есть "уравнения" → поиск неизвестного
- Если есть "площадь", "периметр", "фигуры" → геометрические вычисления
- Если есть "отношения", "пропорции" → работа с пропорциональностью

Б) ТИП ЧИСЕЛ И ОГРАНИЧЕНИЯ:
- "Натуральные" → только 1,2,3,4,5... (НЕ ноль, НЕ отрицательные, НЕ дроби)
- "Целые" → включая отрицательные и ноль
- "Десятичные дроби" → числа с запятой (0.5, 2.75)
- "Обыкновенные дроби" → вид 1/2, 3/4
- "Проценты" → обязательно символ %

В) ФОРМАТ ОТВЕТА:
- Числовые темы → числовой ответ соответствующего типа
- Алгебраические → упрощенное выражение
- Геометрические → число с единицами измерения
- Проценты → число с символом %

ЭТАП 2 - СОЗДАНИЕ ВОПРОСА:
На основе анализа создай вопрос, который:
1. НЕВОЗМОЖНО решить без знания именно этой темы
2. Использует ТОЛЬКО те типы чисел, которые определены в анализе
3. Дает ответ в ПРАВИЛЬНОМ формате для темы
4. Подходит для 5-6 класса (2-3 шага решения)
5. Имеет практический контекст или небольшую историю

ЭТАП 3 - САМОПРОВЕРКА:
Перед финальным ответом проверь:
- Соответствует ли вопрос ТОЧНО теме "{topic}"?
- Правильный ли тип чисел использован?
- Корректный ли формат ответа?
- Нельзя ли решить вопрос знаниями из другой темы?

ФОРМАТ ОТВЕТА:
ВОПРОС: [текст вопроса]
ПРАВИЛЬНЫЙ ОТВЕТ: [правильный ответ]
НЕПРАВИЛЬНЫЙ ОТВЕТ 1: [первый неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 2: [второй неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 3: [третий неправильный вариант]
ОБЪЯСНЕНИЕ: [объяснение решения]"""

    def generate_task_v4(self, topic: str, main_topic: str = None, language: str = 'ru') -> Tuple[Optional[str], Optional[str], Optional[List[str]], Optional[str]]:
        """
        Вариант 3: Мета-промпт с самообучением
        """
        if language == 'kk':
            prompt = self._get_meta_kazakh_prompt(topic, main_topic)
        else:
            prompt = self._get_meta_russian_prompt(topic, main_topic)

        try:
            response_text = self.model.generate_content(prompt).text
            return self._parse_response(response_text, language)
        except Exception as err:
            logging.error(f"Error generating AI task: {err}")
            return None, None, None, None

    def _get_meta_russian_prompt(self, topic: str, main_topic: str) -> str:
        """Мета-промпт с самообучением"""
        return f"""Ты - эксперт по математике. Тебе нужно создать вопрос по теме "{topic}" из раздела "{main_topic}".

МЕТА-ИНСТРУКЦИЯ: Ты должен сам стать экспертом по этой теме, проанализировав её название.

ШАГ 1 - СТАНЬ ЭКСПЕРТОМ ПО ТЕМЕ:
Представь, что ты преподаватель математики. Что бы ты объяснил ученику 5-6 класса по теме "{topic}"?
- Какие основные понятия входят в эту тему?
- Какие операции и вычисления характерны именно для неё?
- Какие типы задач решают в рамках этой темы?
- Какие ошибки часто делают ученики?

ШАГ 2 - ОПРЕДЕЛИ ГРАНИЦЫ ТЕМЫ:
- Что ОБЯЗАТЕЛЬНО должно быть в задаче по этой теме?
- Что НЕ ДОЛЖНО быть (чтобы не перепутать с другими темами)?
- Какой тип чисел использовать?
- В каком формате должен быть ответ?

ШАГ 3 - СОЗДАЙ ЭТАЛОННЫЙ ВОПРОС:
Создай вопрос, который:
- Является "учебником" по теме "{topic}" в миниатюре
- Показывает суть темы через практическую задачу
- Невозможен для решения без знания именно этой темы
- Подходит для возраста 5-6 класс

ШАГ 4 - ПРОВЕРЬ КАЧЕСТВО:
Задай себе вопросы:
- Если ученик решит эту задачу, поймет ли он суть темы "{topic}"?
- Можно ли эту задачу отнести к другой теме? (Если да - переделай!)
- Соответствует ли сложность возрасту?
- Интересна ли задача?

ФОРМАТ ОТВЕТА:
ВОПРОС: [текст вопроса]
ПРАВИЛЬНЫЙ ОТВЕТ: [правильный ответ]
НЕПРАВИЛЬНЫЙ ОТВЕТ 1: [первый неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 2: [второй неправильный вариант]
НЕПРАВИЛЬНЫЙ ОТВЕТ 3: [третий неправильный вариант]
ОБЪЯСНЕНИЕ: [объяснение решения]

Помни: твоя задача - создать вопрос, который на 100% относится к теме "{topic}" и никак иначе!"""

    def _parse_response(self, response_text: str, language: str) -> Tuple[Optional[str], Optional[str], Optional[List[str]], Optional[str]]:
        """Парсинг ответа от AI"""
        if language == 'kk':
            question_pattern = r"СҰРАҚ:\s*(.*?)\s*ДҰРЫС ЖАУАП:"
            correct_answer_pattern = r"ДҰРЫС ЖАУАП:\s*(.*?)(?=\s*ҚАТЕ ЖАУАП 1:|\s*ТҮСІНДІРМЕ:)"
            incorrect_pattern = r"ҚАТЕ ЖАУАП \d+:\s*(.*?)(?=\s*ҚАТЕ ЖАУАП \d+:|\s*ТҮСІНДІРМЕ:|$)"
            explanation_pattern = r"ТҮСІНДІРМЕ:\s*(.*)"
        else:
            question_pattern = r"ВОПРОС:\s*(.*?)\s*ПРАВИЛЬНЫЙ ОТВЕТ:"
            correct_answer_pattern = r"ПРАВИЛЬНЫЙ ОТВЕТ:\s*(.*?)(?=\s*НЕПРАВИЛЬНЫЙ ОТВЕТ 1:|\s*ОБЪЯСНЕНИЕ:)"
            incorrect_pattern = r"НЕПРАВИЛЬНЫЙ ОТВЕТ \d+:\s*(.*?)(?=\s*НЕПРАВИЛЬНЫЙ ОТВЕТ \d+:|\s*ОБЪЯСНЕНИЕ:|$)"
            explanation_pattern = r"ОБЪЯСНЕНИЕ:\s*(.*)"

        try:
            # Parse response
            q_match = re.search(question_pattern, response_text, re.DOTALL | re.IGNORECASE)
            correct_a_match = re.search(correct_answer_pattern, response_text, re.DOTALL | re.IGNORECASE)
            
            question = q_match.group(1).strip() if q_match else None
            correct_answer = self._clean_option_text(correct_a_match.group(1).strip()) if correct_a_match else None
            
            incorrect_options = []
            for match in re.finditer(incorrect_pattern, response_text, re.DOTALL | re.IGNORECASE):
                option_text = match.group(1).strip()
                if option_text:
                    cleaned = self._clean_option_text(option_text)
                    if len(cleaned) <= MAX_OPTION_LENGTH:
                        incorrect_options.append(cleaned)
            
            e_match = re.search(explanation_pattern, response_text, re.DOTALL | re.IGNORECASE)
            explanation = e_match.group(1).strip() if e_match else None
            
            # Validate response
            if not (question and correct_answer and explanation and len(incorrect_options) >= 1):
                return None, None, None, None
            
            if len(correct_answer) > MAX_OPTION_LENGTH:
                return None, None, None, None
            
            return question, correct_answer, incorrect_options, explanation
            
        except Exception as err:
            logging.error(f"Error parsing AI response: {err}")
            return None, None, None, None

    @staticmethod
    def _clean_option_text(text: str) -> str:
        """Clean option text by removing unnecessary words and extra spaces."""
        # Remove words like "ANSWER", "CORRECT", "INCORRECT", etc. in Russian and Kazakh
        text = re.sub(r'(ОТВЕТ|ПРАВИЛЬНЫЙ|НЕПРАВИЛЬНЫЙ|ВЕРНО|НЕВЕРНО|НЕВОЗМОЖНО ОПРЕДЕЛИТЬ|НЕВОЗМОЖНО СКАЗАТЬ|НЕ МОГУ ОПРЕДЕЛИТЬ|НЕ МОГУ СКАЗАТЬ|НЕ УВЕРЕН|НЕ ЗНАЮ|ЗАТРУДНЯЮСЬ ОТВЕТИТЬ|СЛОЖНО СКАЗАТЬ|НУЖНО ПОДУМАТЬ|ПРОВЕРЮ ЕЩЕ РАЗ|ДРУГОЙ ВАРИАНТ|ЭТО НЕВЕРНО|НЕПРАВИЛЬНЫЙ ВАРИАНТ|ПРАВИЛЬНЫЙ ВАРИАНТ|ВЕРНЫЙ ОТВЕТ|НЕВЕРНЫЙ ОТВЕТ|ЖАУАП|ДҰРЫС|ҚАТЕ|ДҰРЫС ЖАУАП|ҚАТЕ ЖАУАП|АНЫҚТАУ МҮМКІН ЕМЕС|АЙТА АЛМАЙМЫН|БІЛМЕЙМІН|СЕНІМДІ ЕМЕСПІН|\\s*\\(.*?\\))', '', text, flags=re.IGNORECASE)
        # Remove multiple spaces
        text = re.sub(r'\s+', ' ', text)
        # Remove leading and trailing spaces
        return text.strip()

    def _get_contextual_kazakh_prompt(self, topic: str, main_topic: str) -> str:
        """Контекстуальный промпт для казахского языка"""
        return f"""Сіз 5-6 сынып оқушыларына арналған математикалық есептер жасау сарапшысысыз.

МІНДЕТ: "{topic}" тақырыбы бойынша "{main_topic}" бөлімінен сұрақ жасау.

1-КЕЗЕҢ - ТАҚЫРЫПТЫ АВТОМАТТЫ ТАЛДАУ:
"{topic}" тақырыбын талдап, автоматты түрде анықтаңыз:

А) МАТЕМАТИКАЛЫҚ САЛА:
- Егер атауда "сандар", "натурал", "бүтін" сөздері болса → белгілі бір түрдегі сандармен жұмыс
- Егер "бөлшектер", "ондық", "жай" болса → бөлшектермен жұмыс
- Егер "пайыздар" болса → міндетті % қолдану
- Егер "өрнектер", "жеңілдету", "жақшалар" болса → алгебралық түрлендірулер
- Егер "теңдеулер" болса → белгісізді іздеу
- Егер "аудан", "периметр", "фигуралар" болса → геометриялық есептеулер

Б) САНДАР ТҮРІ ЖӘНЕ ШЕКТЕУЛЕР:
- "Натурал" → тек 1,2,3,4,5... (нөл ЖОК, теріс ЖОК, бөлшек ЖОК)
- "Бүтін" → теріс және нөлді қоса
- "Ондық бөлшектер" → үтірлі сандар (0.5, 2.75)
- "Жай бөлшектер" → 1/2, 3/4 түрі
- "Пайыздар" → міндетті % белгісі

В) ЖАУАП ФОРМАТЫ:
- Сандық тақырыптар → сәйкес түрдегі сандық жауап
- Алгебралық → жеңілдетілген өрнек
- Геометриялық → өлшем бірліктері бар сан
- Пайыздар → % белгісі бар сан

2-КЕЗЕҢ - СҰРАҚ ЖАСАУ:
Талдау негізінде мынадай сұрақ жасаңыз:
1. Дәл осы тақырыпты білмей шешу МҮМКІН ЕМЕС
2. Талдауда анықталған сан түрлерін ҒАНА қолданады
3. Тақырыпқа ДҰРЫС форматта жауап береді
4. 5-6 сыныпқа жарамды (2-3 қадам шешім)
5. Практикалық контексті немесе кішкентай тарихы бар

ФОРМАТ:
СҰРАҚ: [сұрақ мәтіні]
ДҰРЫС ЖАУАП: [дұрыс жауап]
ҚАТЕ ЖАУАП 1: [бірінші қате нұсқа]
ҚАТЕ ЖАУАП 2: [екінші қате нұсқа]
ҚАТЕ ЖАУАП 3: [үшінші қате нұсқа]
ТҮСІНДІРМЕ: [шешімнің түсіндірмесі]"""

    def _get_meta_kazakh_prompt(self, topic: str, main_topic: str) -> str:
        """Мета-промпт для казахского языка"""
        return f"""Сіз математика сарапшысысыз. "{topic}" тақырыбы бойынша "{main_topic}" бөлімінен сұрақ жасау керек.

МЕТА-НҰСҚАУЛЫҚ: Сіз осы тақырып бойынша сарапшы болуыңыз керек, оның атауын талдау арқылы.

1-ҚАДАМ - ТАҚЫРЫП БОЙЫНША САРАПШЫ БОЛЫҢЫЗ:
Сіз математика мұғалімісіз деп елестетіңіз. "{topic}" тақырыбы бойынша 5-6 сынып оқушысына не түсіндірер едіңіз?
- Бұл тақырыпқа қандай негізгі ұғымдар кіреді?
- Қандай амалдар мен есептеулер дәл осыған тән?
- Осы тақырып аясында қандай есептер шешеді?
- Оқушылар қандай қателер жиі жасайды?

2-ҚАДАМ - ТАҚЫРЫП ШЕКАРАСЫН АНЫҚТАҢЫЗ:
- Осы тақырып бойынша есепте не МІНДЕТТІ болуы керек?
- Не БОЛМАУЫ керек (басқа тақырыптармен шатастырмау үшін)?
- Қандай сан түрін қолдану керек?
- Жауап қандай форматта болуы керек?

3-ҚАДАМ - ҮЛГІЛІК СҰРАҚ ЖАСАҢЫЗ:
Мынадай сұрақ жасаңыз:
- "{topic}" тақырыбының кішкентай "оқулығы" болып табылады
- Практикалық есеп арқылы тақырыптың мәнін көрсетеді
- Дәл осы тақырыпты білмей шешу мүмкін емес
- 5-6 сынып жасына сәйкес

4-ҚАДАМ - САПАНЫ ТЕКСЕРІҢІЗ:
Өзіңізге сұрақтар қойыңыз:
- Оқушы бұл есепті шешсе, "{topic}" тақырыбының мәнін түсінер ме?
- Бұл есепті басқа тақырыпқа жатқызуға бола ма? (Егер болса - қайта жасаңыз!)
- Күрделілік жасқа сәйкес пе?
- Есеп қызықты ма?

ФОРМАТ:
СҰРАҚ: [сұрақ мәтіні]
ДҰРЫС ЖАУАП: [дұрыс жауап]
ҚАТЕ ЖАУАП 1: [бірінші қате нұсқа]
ҚАТЕ ЖАУАП 2: [екінші қате нұсқа]
ҚАТЕ ЖАУАП 3: [үшінші қате нұсқа]
ТҮСІНДІРМЕ: [шешімнің түсіндірмесі]

Есте сақтаңыз: сіздің міндетіңіз - "{topic}" тақырыбына 100% жататын және басқаша болмайтын сұрақ жасау!""" 